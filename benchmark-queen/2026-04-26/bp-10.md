# Batch-push axis — `bp-10` (1 001 partitions, **batch=10**, low load 1×50)

**Run:** `2026-04-25T13:20:08Z → 13:36:24Z` (15 min)

## Setup

| | |
|---|---|
| `MAX_PARTITION` | 1000 |
| `MSGS_PER_PUSH` | **10** |
| Producer | 1 × 50 conns |
| Consumer | 1 × 50 conns, batch=100 |

## Producer — autocannon

| | |
|---|---|
| Total push requests | **3 515 078** |
| Total messages pushed | **35 150 780** |
| Throughput | **3 906 req/s = 39 060 msg/s** |
| Latency p50 / p99 / avg / max | **11 / 38 / 12.3 / 664 ms** |
| Errors / timeouts | 0 / 0 |

## Consumer — autocannon

| | |
|---|---|
| Total pop requests | 818 896 |
| req/s | 910 |
| Effective msg/s (req × 42.29 batch) | **38 481 msg/s** |
| Latency p50 / p99 / avg / max | **16 / 356 / 54.5 / 8 272 ms** |
| Errors / timeouts | 0 / 0 |
| Server-reported pop batch eff. | **42.29 msg/req** |

## Server-side window aggregates

| | |
|---|---|
| Steady-state ingest | **38 898 msg/s** |
| Steady-state pop | **38 351 msg/s** |
| Max ingest (1-min) | **40 182 msg/s** |
| Max msg dwell-time | 564 482 ms ≈ 9 min 24 s |
| Queen CPU user % avg | **744 %** → ~7.4 vCPU |
| Queen RSS max | 52.1 MB |
| DB pool active avg / max | 2.38 / 2.4 |
| Errors | 0 |
| `batchEfficiency` push / pop | 10.00 / **42.29** |

## Postgres at end

- Cache hit: **100 %**
- `messages` size: **14 GB** (6 104 MB heap + 8 249 MB indexes)
- Postgres mem: 19 GiB (during run)

## Queue final state

| | |
|---|---|
| Total | 35 151 280 |
| Completed | 34 628 820 |
| Pending end | 522 460 (**1.5 %**) |

## Headline takeaways

- **Massive throughput jump from `bp-1`**: 5 796 → **39 060 msg/s** = **6.74× more messages** at the same client load (1 × 50 conns), just by batching 10 items per push.
- **Per-request latency increase is small**: p99 went 13 → 38 ms (~3× higher), but throughput went up 6.74× — that's a **2.2× efficiency gain** per request.
- **Postgres CPU went up a lot** (~3.5 → ~6 vCPU during run) because it's now writing 39 k rows/s vs 5.8 k. But still handling it at 100 % cache hit, only 2.4 active connections.
- **Consumer pop batch efficiency = 42.29** (vs 5.36 at batch=1). At 39 k/s ingest, partitions stay deep enough to fill ~42 of the 100-msg pop request — more than enough to keep up.
- **Pending end = 1.5 %** — consumer keeping up nicely.
- **0 errors anywhere**.

This run reproduces the long-running setup (1×50 batch=10 MP=1000) on a **fresh DB**, hitting **~39 k msg/s** vs the long-running test's **~28 k msg/s** (after 14 h of accumulated state and 35 GB `messages` table). Demonstrates how cold-state performance is meaningfully better than warm-state under sustained load.
